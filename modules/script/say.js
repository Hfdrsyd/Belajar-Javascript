export function sayHello(name) {//export dilakukan dengan menulis ecara explicit
  console.info(`Hello ${name}`);
}
export function sayGoodBye(name) {
  console.info(`Gudbay ${name}`);
}
const name = "eko";
